# WU-40 Stage B — Codex Full AI-UAT

- WU / Jira: **WU-40 / KAN-58**
- 実施日: 2026-09-14
- QA URL: `https://sur-context-basin-concert.trycloudflare.com`
- Stage B開始時deployed SHA: `14f292e0c5432a3814f83e26bf52ff73faacb930`
- 最終deployed code SHA: `57aec7cc7ce8486038e92fcad306166b9e8780fa`
- Branch: `feat/wu23-40-phase15-full-20260914`
- Worktree: `/Users/naoki/Documents/Codex/2026-09-14/digital-map-wu-23-through-wu/work/digital-map`
- 最終判定: **READY FOR HUMAN UAT**（Human UAT自体は未実施、WU-40は未完了）

## 実行環境・証跡

- Browser: agent-browser ChromiumおよびCodex browser automation。
- Viewports: desktop `1280 × 800` / `1280 × 720`、mobile portrait `390 × 844`、mobile resize/landscape `844 × 390`。
- Windows QA: PostgreSQL、fake mail adapter、local Public Snapshot storage、既存Quick Tunnel。
- Screenshot evidence: [`docs/qa/evidence/wu40-stageb/`](./evidence/wu40-stageb/)。最終証跡30 PNGを保存。
- PDF evidence: `pdf-a4-portrait-final.png`、`pdf-a3-landscape-final.png`。元PDFはQA作業用一時領域でparse/render検証した。
- Production、main、rollback backupは未変更。`C:\DigitalMap\backups\wu23-40-b2581f3`を保持。
- Resend、Cloudflare R2、本番Domain/DNS/TLSは **EXTERNAL ACTIVATION PENDING**。QA adapterの実装経路を検証した。

## Test fixtures

`scripts/qa/stageb-fixtures.ts`と管理UIを使い、次の隔離fixtureをWindows QAに準備した。

- Tenant A: `cmtygyjoe0000h8vadv3cau9l` / `WU40 Tenant A`。
- OWNER: `codex-owner-a@qa.invalid`。
- MEMBER + Map EDITOR: `codex-editor-a@qa.invalid`。
- Spot Editor: `codex-spot-a@qa.invalid`、`codex-spot2-a@qa.invalid`。invite acceptance用new User `codex-stageb-new2@qa.invalid`。
- Map A1: `demo-arimatsu-map` / `team-demo-arimatsu`（Editor割当、public releaseあり）。Map A2: `cmtygyjq60002h8va9z5ip0o6`（Editor未割当）。
- A1: Illustration Floor `demo-arimatsu-floor`、Categories、dynamic fields、Media、Decoration、positioned public Spots 10件、unpositioned/duplicate-name/CSV Spotを含む管理Spot 15件以上。
- Tenant B: `wu40-tenant-b`、OWNER B、Map B1 `wu40-map-b1`。Tenant isolation negative test専用。
- Signup: `codex-stageb-signup-20260914@qa.invalid`からpending intentを作成し、検証後 `WU40 Signup Tenant` とOWNERを作成。
- Public READY releases: current `cmu0y07460008kwva0pgyga47`、rollback対象 `cmu0x6v0z000l7ovancsilx1k`、旧release `cmu0sdr1u0000zkva6ad3p4jy`。

生token、password、cookieはreport、AuditEvent、MailDelivery、screenshotへ保存していない。招待・reset・signup verificationのtoken-bearing URLはbrowser session内だけで使用した。

## 最終automated gateとWindows deploy

| Gate | Local `57aec7c` | Windows `57aec7c` |
| --- | --- | --- |
| Full tests | 62 files / 405 tests PASS | 62 files / 405 tests PASS |
| Typecheck | PASS | PASS |
| Prisma validate | PASS | PASS |
| Prisma generate | PASS | PASS |
| Production build | PASS | PASS |
| Migration status | 28 folders | 28/28 applied、schema current |
| Health | — | `/api/health`: `ok` |
| Ready | — | `/api/ready`: `ready`, database=true, mail=fake |

Windowsはcommit archiveを新release folderへ展開し、frozen install、Prisma、full tests、typecheck、build、migration statusの全成功後にactive pathを切り替えた。cloudflared、PostgreSQL、rollback backupは停止・削除していない。

Stage Aの「local 61/397、Windows 61/396」差異はplatform skipではない。Windows full suiteが古い`d24b01f`、localがSnapshot/PDF test追加後の`14f292e`だったためである。同一SHAでの比較は`98ca68c`が401/401、`a3c338b`が402/402、最終`57aec7c`が405/405となり、差異は完全に解消した。

## OWNER end-to-end journey

| Step | 実行結果 |
| --- | --- |
| Login / organization context | OWNERでloginしTenant A、4 Maps、role別membersを確認。PASS |
| Map作成 / Illustration setup | UIからMap作成、setup route、Illustration Map選択を確認。PASS |
| Floor illustration | 1448×1086 PNGをuploadし、processing完了後にFloor作成。reload後も保持。PASS |
| Category / Spot Fields | Category、usage、English translation欄、standard/dynamic field設定を編集・保存。PASS |
| Spot / Media | Spot作成、既存Media再利用、新規photo、field値を保存。AIUAT-002最終rerunでは`AIUAT-002 Final Rerun`作成後、同一操作で詳細URLと詳細formへ遷移。PASS |
| PIN / Decoration | PIN配置、再配置、importance、size、preset/custom UI、Decoration配置画面を確認。PASS |
| CSV / duplicates / bulk | locale-qualified CSV preview/import、duplicate warning、bulk Category Add/Removeを実行。PASS |
| Dirty / save feedback | dirty navigation guard、double save抑止、保存成功表示、reload後永続化を確認。PASS |
| Publish / republish / rollback | publish、admin live変更、republish、old READY rollback、latestへroll-forward。PASS |
| Analytics / Audit | daily Map 3、Top Spot 2をrender。Audit direct URL、reload、client navigationでevent rowsを確認。AIUAT-003 PASS |
| PDF | A4/A3 × portrait/landscapeを生成・parse・render。PASS |

永続化はreload、画面navigation、Back/Forward、logout/loginの各境界で確認した。

## Map EDITOR end-to-end journey

- Assigned Map A1へloginし、Spot、Category、Floor、Field、Media、Decoration、publish、Spot Revision reviewを実行できた。PASS。
- unassigned Map A2のdirect pageは404、mutation APIも404。Tenant B Map B1は404。PASS。
- organization member管理とAudit Logはserver 403。UI非表示だけに依存しない。PASS。
- OWNER-only destructive operationはserver拒否、last-OWNER guardも保持。PASS。
- OWNERがA1のMapMemberをremoveした直後、開いたEditor browserの次のprotected requestは権限喪失。stale cookieでprivilegeは継続しなかった。再assignment後だけ復帰し、remove/assign AuditEventも存在。PASS。

## Spot Editor end-to-end journey

1. OWNERがSpot-targeted inviteを発行し、new Userがaccept。Tenant A MEMBER作成、target Spot assignment、invite single-useを確認。PASS。
2. Spot Editorには担当Spotだけが表示され、Map/Tenant管理、Media Library、unrelated Spotへのdirect accessは拒否。PASS。
3. allowed text/Custom Fieldを編集し、新規photoをpending revisionへupload、selection変更して保存。live/publicは旧値・旧photoのまま。PASS。
4. Map EDITOR/OWNER review diffからapprove。allowed fields/photo orderだけliveへ反映し、republish後にpublicへ反映。PASS。
5. 2件目はrejectし、live/public不変。PASS。
6. pending作成後にOWNERがlive Spotを変更。approveは409でoverwriteせず、最終UIは「公開中のSpotが更新されたため承認できません」と説明し、reject可能。PASS。
7. assignee replacement後、old Editorのnext requestは拒否。最終直接URLrerunでは権限喪失alertだけを表示し、editable `form` countは0。PASS。
8. old authorのpendingは残り、新assigneeはtakeover/新規pending不可。review/reject後にのみ新assigneeが編集可能。PASS。
9. invite、assignment/replacement、revision created/submitted/approved/rejectedのAuditEventをOWNER logで確認。PASS。

## Anonymous public journey

- Desktop/mobileでpublished Map、Category filter、DOM PIN、Spot selection、Bottom Sheet、photo、public Custom Fields、website linkを操作。PASS。
- keyboardで`summary`を開き、10件のSpot list buttonsからpointerなしでSpotへ到達。marker EnterでBottom Sheetを開き、Escapeで閉じた後のactive elementは元の`BUTTON[data-spot-id=demo-arimatsu-01]`。PASS。
- mobile `390×844`でBottom Sheet open中に`844×390`へresizeしてもdialog、close、Map controlにoverflow/overlapなし。PASS。
- ja/enを反復し、Back/Forward/reload後もexplicit queryが優先。English headingは`Arimatsu Heritage Map`、translated Spotは`Arimatsu Tie-Dyeing Museum`、missing holiday translationは日本語fallback。PASS。
- analytics routeをbrowserでabortしてreloadしてもpublic Mapは即時描画され、操作をblockしなかった。PASS。
- DOM検査: `brokenImages=[]`。unpublished/unpositioned CSV Spot、Tenant B Spot、internal field、pending revisionはbody/Snapshotに不在。PASS。
- English metadata: canonicalは`/team-demo-arimatsu?lang=en`、hreflang ja/en/x-default、`og:locale=en_US`、OG imageはimmutable release asset。PASS。

## Mandatory cross-feature scenarios

### A. Revision × Media × Snapshot — PASS

- Spot Editor uploadでnormal Tenant MediaAssetとpending `SpotRevisionPhoto`を作成。GC dry-run中、pending assetはcandidate 0。live/publicはapproveまで旧値。
- approveでlive photo relationsをatomic反映し、revision-only relationsを解放。republishしたreleaseへWebP byteをcontent-hash keyでcopy。
- 元Media relation変更後も旧READY releaseのasset file/HTTP responseを維持。Snapshot testでもoriginal detach/delete後のrelease独立性を再現。

### B. i18n × CSV × Snapshot × SEO — PASS

- locale-qualified CSVで日本語`AI-UAT CSV 店`、English translationをpreview/import。日本語only値のfallbackも確認。
- publish manifestにlocale/translationsを含み、`?lang=en`でEnglish、missing値はja fallback。
- canonical、hreflang ja/en/x-default、`og:locale`、immutable OGP imageをDOMで確認。unsupported localeはjaへfallback。

### C. Auth × Session × Audit — PASS

- Editor browserを開いたままOWNERがMap authorizationをremove。次のprotected requestはserver拒否、stale cookieで継続不可。
- unrelated Tenant membershipは保持。remove/reassignのAuditEventは同じTenantに存在し、EditorからAudit readは403。

### D. Publish failure × rollback — PASS

- pointer switch前のobject write失敗をWindows `public-release.test.ts`で注入し、old `current.json`不変を確認。
- 実QAでsuccess publish後、`cmu0x6v0z...`へrollbackし旧contentを表示、`cmu0y074...`へroll-forward。READY releaseはrewriteされない。

### E. Media GC × pending revision × public release — PASS

- live relationとPENDING revision relationをGCが保護。reject/approve後はrevision photo relationが削除され、zero-reference assetだけが7-day grace後candidateになる。
- dry-runは削除0、explicit deleteだけが物理削除可能。shared assetとimmutable release assetは残存。

### F. Signup × Legal × Email × Security — PASS

- fake mailでsignup requestを作成。verification前はSignupIntentのみでTenant/accessible orgなし。
- delivery status SENT、linkはQA `ADMIN_BASE_URL`。MailDelivery/Audit/logにraw token/bodyなし。
- verifyでUser + Tenant + OWNER + Terms/Privacy `2026-09-14-v1` acceptanceをatomic作成。token reuseは400。
- signup 5回/hourを超えた6回目は429。existing User new Tenantとpending unrelated invitationの非消費も確認。

### G. PDF × public URL — PASS

- A4 portrait `595.276×841.89 pt`、A4 landscape `841.89×595.276 pt`、A3 portrait `841.89×1190.55 pt`、A3 landscape `1190.55×841.89 pt`を生成・parse。
- current published releaseのFloor/Decoration/PIN 1–10/Category legend/title/QRをrender確認。PINはnormalized x/yに分散し、clipなし。
- QR payloadは`PUBLIC_BASE_URL + /team-demo-arimatsu`とexact一致。unpublished previewはlabelあり/QRなし。multi-floorとlong legend continuation pageはWindows testでPASS。

## Phase 1 regression — Axis B

| Phase 1機能 | Evidence | Result |
| --- | --- | --- |
| IMAGE normalized x/y | positioned Spotsをeditor/public/PDFで確認、migration/position tests | PASS |
| Illustration replacement keeps position | replacement contract test、existing positionsの再render | PASS |
| Georeference add/edit/remove | two-point設定済みUI、API add/edit/remove tests | PASS |
| Reposition/unplace | dedicated editorとserver position endpoints | PASS |
| Media Library reuse / Spot photos | pickerで既存画像、upload、reorder/remove、reload | PASS |
| Category custom icon / Custom PIN | Category editor、PIN design、alpha/variant tests | PASS |
| Dynamic Field Definitions/detail | settings、Spot edit、public detail/visibility tests | PASS |
| CSV import | old ja formatと`[en]` preview/import | PASS |
| Decoration | editor、public release、PDF inclusion | PASS |
| PIN importance/size/density | editor controls、public markers、density tests | PASS |
| Category usage/bulk Add/Remove | usage表示、bulk mutation/rollback tests | PASS |
| Spot search/filter/sort/state | 15件以上のlistでfilters、history/state tests | PASS |
| double-click zoom | Map interaction regression test | PASS |
| dirty-state guard | navigation/reload/back操作とguard tests | PASS |
| duplicate warning | duplicate-name fixturesとdialog | PASS |
| save feedback | repeated save抑止、success/error states | PASS |
| mobile Bottom Sheet/public Map | 390×844、844×390、keyboard/Escape/focus | PASS |

結論: WU-23〜WU-39による未解決Phase 1 regressionはない。

## Exploratory / abuse testing

| Case | Result |
| --- | --- |
| repeated/double save、rapid clicks | processing/disabled guard、single mutation、consistent feedback。PASS |
| dirty navigation、reload、Back/Forward while editing | guard表示。discard/continue後の状態が一貫。PASS |
| required empty | native/schema validationでmutationなし。PASS |
| long name/description、mixed ja/en | 保存・fallback・responsive wrapping。PASS |
| duplicate names、zero Categories/photos | warningは出るが許可仕様を維持、zero states描画。PASS |
| many photos | reorder/removeとresponsive overflow test。PASS |
| ~100 Spots | deterministic density/filter testで100件相当を実行。実QA公開fixtureは10件、管理list 15件以上。PASS |
| invalid extension / MIME mismatch / oversized | signature/10MiB/server validationで拒否。PASS |
| reused/expired/revoked invite、reused/expired reset | 400、membership/password再変更なし。PASS |
| stale session after password/role change | next protected request拒否。PASS |
| tampered Tenant/Map/Spot IDs | 403/404、Tenant B dataなし。PASS |
| pending while live changes | approve 409、no overwrite、UI説明、reject可能。PASS |
| publish failure | old pointer/current release維持。PASS |
| analytics ingest failure | browser route abort中もpublic UI描画。PASS |
| repeated locale switch | explicit query/history/fallback一貫。PASS |
| mobile resize + Escape | no overlap、close、marker focus return。PASS |
| PDF long legend | continuation page、no clipping。PASS |

## Visual QA

以下を最終QAまたは同一Stage B runでcaptureし、overflow、clipping、overlap、responsive break、spacing、contrast、z-index、touch target、selected/loading/error stateを目視確認した。

- OWNER dashboard、Map setup、Map editor、Floor manager、Georeference editor。
- Spot list、Spot edit、Field settings、Category management、Media picker、Decoration editor。
- member/invitation management、Spot Editor list/edit、revision review/stale conflict、Audit Log、Analytics、publish/release。
- public desktop English、public mobile、Bottom Sheet、resize-open state、Terms、Privacy、login、onboarding/signup。
- A4 portraitおよびA3 landscape PDF pages。

公開Map、Bottom Sheet、PDF、管理主要画面にclip/overlap/z-index/破損画像はない。selected、disabled、error、publish statusは色以外のtext/badgeでも識別可能。token-bearing acceptance URLは秘密保持のためscreenshotへ残さず、acceptance UIはbrowser session内で目視した。

Remaining visual P3: Field settingsの一部compact「保存」buttonがdesktopで2行にwrapする。操作、accessible name、hit area、内容の判別には影響せず、core P2ではない。将来のpolish候補として受容。

## Accessibility

- Actual axe-core 4.12.1、WCAG 2 A/AA + 2.2 AA tags:
  - Public initial desktop: **0 violations**。
  - Public Spot Bottom Sheet mobile: **0 violations**。
  - Terms: **0 violations**。
  - Privacy: **0 violations**。
  - Admin login: **0 violations**。
  - Signup/onboarding: **0 violations**。
- Public axeはMap image/canvas背景のためcolor判定不能、marker overflow geometryのためtarget-size判定不能を`incomplete`として返した。violationではなく、実CSSでmarker buttonは60×60px、illustration markerもmin 32×48px、主要controlsは24px以上を確認し、目視contrastもPASS。
- Keyboard: locale selector、Category buttons、accessible Spot list、marker Enter、Bottom Sheet Escape、close button、linksを操作。visible focusあり。
- Bottom Sheet: dialog title relation、focus trap、Escape close、元marker focus returnを実測。
- Decorationはassistive technologyからhidden、photoには意味のあるalt、icon buttonsにはaccessible name。
- 最終Windows automated: `accessibility.test.ts` 5/5、`accessibility-source.test.ts` 2/2を含む全405 tests PASS。

Critical/serious automated violationは0。a11y gateはPASS。

## Security / RBAC

- OWNER、assigned Map EDITOR、Spot Editorのpositive/negative matrixをbrowserとserver responseで検証。
- Tenant B Map/Spot/Category/Media/Field/Decoration IDsへのtamperingは403/404、response/bodyにTenant B private dataなし。
- unassigned Map EDITORのpage/mutation、Spot Editorのforbidden domains、Audit non-OWNER readをserver拒否。
- role removal、password change/reset、membership removalはauthoritative session version/live membership checkでstale accessを無効化。
- CSRF Origin、Secure production cookie、CSP/HSTS/headers、upload signature/size/path hardening、rate limit boundaryは最終Windows suiteでPASS。
- Audit/Mail/logにpassword、hash、raw invite/reset/verification token、cookie、API key、full mail bodyなし。

## Defects found, fixes, rerun evidence

| ID | Severity | Defect | Fix commit | Final Windows rerun |
| --- | --- | --- | --- | --- |
| AIUAT-001 | P1 | Map作成が無反応 | `2a120039` | browser Map作成 + DB persistence PASS |
| AIUAT-002 | P2 | Spot作成後URLだけ変わりformが残る | `ff4060ce` | `AIUAT-002 Final Rerun`が詳細URL/詳細formへ即時遷移 PASS |
| AIUAT-003 | P2 | Audit direct URL/reloadが401 | `2a323ce3` | direct URL、reload、event rows描画 PASS |
| AIUAT-004 | P1 | PDF PINがnormalized座標を使わず重なる | `2da9d245` | 4 formats生成、A4/A3 renderで1–10分散 PASS |
| AIUAT-005 | P1 | Spot assignee routeがSpot detail routeと競合 | `c164aa00`, `e0d58ec8` | detail/assignee/Spot Editor browser route PASS |
| AIUAT-006 | P1 | immutable public asset routeが全404 | `083b148d` | Floor PNG/photo WebP/OG image HTTP 200、brokenImages 0 PASS |
| AIUAT-007 | P2 | approved/rejected revision photo relationが残りGCを過保護 | `98ca68ca` | revisionPhotos 3→0、PENDINGだけ保護、release asset保持 PASS |
| AIUAT-008 | P2 | Bottom Sheet Escape後focusが`BODY` | `d4f2749a`, `a3c338bd` | mobile Enter→Escape後、元marker buttonへfocus PASS |
| AIUAT-009 | P2 a11y | legal pageにdocument `lang`がなくaxe serious | `d29d02ce` | Terms/Privacy/login/signup actual axe 0 violations PASS |
| AIUAT-010 | P2 UX | stale approveと旧Spot Editor権限喪失に画面説明なし | `57aec7cc` | 409説明表示、old Editor alert、editable form 0 PASS |

Open P0: 0。Open P1: 0。Open core P2: 0。Remaining P3は上記Field settings button wrapのみ。

## Migration / backup / dependency status

- 28 migration foldersを順序監査。fresh disposable deployとbaseline 17 migrationsからWU-23〜39のrepresentative upgradeはStage A PASS。Windows QAは28/28 current。
- Windows IMAGE spatial auditはinvalid dimensions/reference、partial coordinates、out-of-range positionすべて0。
- Backup: `C:\DigitalMap\backups\wu23-40-b2581f3`にcustom DB dump + metadata/checksum、Media archive + manifest/checksumを保持。rollbackはDB/Media restore、pre-WU env復元、prior release `3d4ee73749c0ae23f5cacebd9c752d55b337adc3`へrepoint。
- Production dependency audit: Critical/Moderate/Low 0。Prisma configのtransitive `deepmerge-ts` High 1件は既知。修正候補がPrisma 8 RC major overrideのため、安全でないoverrideは適用していない。

## Remaining external items

- Resend live activation: **PENDING**。fake providerでinvite/reset/verification lifecycleとdelivery statusを検証。
- Cloudflare R2 live activation: **PENDING**。local adapterでimmutable build/pointer/rollback/cache/asset independenceを検証。
- Production Domain/DNS/TLS: **PENDING**。production configはHTTPS absolute URL、no localhost、no Quick Tunnelをstrict validation。QAは既存Quick Tunnelを維持。

## Final Stage B verdict

- Axis A（WU-23〜WU-39 integration integrity）: PASS。
- Axis B（whole-product / Phase 1 regression）: PASS。
- OWNER、Map EDITOR、Spot Editor、public visitor journeys: PASS。
- Mandatory cross-feature scenarios A–G: PASS。
- Exploratory、visual、accessibility、security/RBAC: PASS。
- Windows QA final code SHA `57aec7cc7ce8486038e92fcad306166b9e8780fa`: stable、health/readiness PASS。
- Human UAT status: **PENDING HUMAN EXECUTION**。
- main merge / Production deploy / rollback deletion: 未実施。

**READY FOR HUMAN UAT**
