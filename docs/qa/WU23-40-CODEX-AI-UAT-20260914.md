# WU-40 Stage B — Codex AI-UAT

- WU / Jira: **WU-40 / KAN-58**
- 実施日: 2026-09-14
- QA URL: `https://sur-context-basin-concert.trycloudflare.com`
- 開始時deployed SHA: `14f292e0c5432a3814f83e26bf52ff73faacb930`
- 途中再deploy SHA: `2a12003951921f3c14dd3a080394161e788d9a71`
- 報告書作成時branch HEAD: `2a323ce38decac226bad9a215085a5e591234453`
- 最終判定: **AI-UAT NOT READY**

## 実行環境

- Browser: Codex in-app browser（Chromium系）、desktop 1280 × 720。
- Windows QA: PostgreSQL、fake mail adapter、local Snapshot storage、既存Quick Tunnel。
- 外部activation: Resend、Cloudflare R2、本番Domain/DNS/TLSはPENDING。
- Production/main/rollback backupは未変更。`C:\DigitalMap\backups\wu23-40-b2581f3`を保持。

## Test fixtures

`scripts/qa/stageb-fixtures.ts`をWindows QA上で実行し、既存デモTenantをTenant Aとして次を隔離作成した。

- Tenant A: OWNER `codex-owner-a@qa.invalid`、Map EDITOR `codex-editor-a@qa.invalid`、Spot Editor 2名、invite acceptance用のmembership未所属User。
- Map A1: `demo-arimatsu-map` / `team-demo-arimatsu`（Editor割当、公開releaseあり）。Map A2: `cmtygyjq60002h8va9z5ip0o6`（Editor未割当）。
- A1: 既存10 Spotsに加え、同名のpositioned/unpositioned Spot 2件、専用Category、既存field定義。
- Tenant B: `wu40-tenant-b`、OWNER B、Map B1 `wu40-map-b1`、非公開Spot。
- OWNER browser journeyでMap `cmu0tex4v0000z4va0wzosjdh`、Floor `cmu0u43gc000az4vaejxxpv78`、Spot `cmu0u55wx000cz4vam7v3kkdm`を実際のUIから作成。

## 実ブラウザ user journey / persistence evidence

| Journey | Evidence | Result |
| --- | --- | --- |
| OWNER login / organization context | QA専用OWNERでloginし、Tenant A名、4 Maps、role別membersをrender確認 | PASS |
| Map creation | `14f292e`で選択・入力・clickしても画面無反応。原因修正後の`2a12003`でMap作成、setup URLとDB永続化を確認 | PASS after fix |
| Illustration Floor | 1448×1086 PNGをbrowser file chooserからupload。processing完了後にFloor作成、一覧とreload後の永続化を確認 | PASS |
| Category | `飲食`をUI作成、usage 0とEnglish任意欄を確認 | PASS |
| Spot | Floor/Category/importance/ja+en混在説明を入力し作成。URLは詳細へ変わったが画面本体が作成フォームのまま残った。reload後は作成済みSpot詳細が表示 | FAIL (P2; fixed locally, rerun pending) |
| Audit Log | 組織画面からclient navigationは表示成功。direct URLでは401。 | FAIL (P2; fixed locally, rerun pending) |
| Map EDITOR full journey | 未実行 | NOT RUN |
| Spot Editor full lifecycle | 未実行 | NOT RUN |
| Anonymous desktop/mobile public journey | 未実行 | NOT RUN |

ReloadによりMap/Floor/Category/Spotが残ることを確認した。Back/Forward、logout/login後の全項目再確認は未完了。

## Axis A — WU-23〜WU-39 integration integrity

実測済みのAuth/RBAC → Map setup → Media upload/variants → Floor → Category → Spot作成の連鎖で、下記の統合欠陥を発見した。Revision×Media×Snapshot、i18n×CSV×SEO、Auth×Session×Audit、publish failure×rollback、GC、Signup×Legal×Email、PDF×public URLは、Stage A自動テスト証跡はあるが指定された統合ブラウザシナリオをまだ実行していないためPASSにしていない。

## Axis B — whole-product / Phase 1 regression

実ブラウザでMap作成、Illustration upload、MediaAsset作成、Floor作成、Category作成、Spot作成、importance選択、PIN design表示、Spot photo/media picker表示を確認した。IMAGE position、illustration replacement、georeference add/edit/remove、reposition/unplace、Media reuse、photos、custom Category/PIN、dynamic fields/detail、CSV、Decoration、density、bulk category、list state、double-click zoom、dirty guard、duplicate warning、mobile Bottom Sheetの完全な回帰ジャーニーは未完了であり、「WU-23〜39がPhase 1を壊していない」と最終断定できない。

## Mandatory cross-feature scenarios

| Scenario | Result |
| --- | --- |
| Revision × Media × Snapshot | NOT RUN |
| i18n × CSV × Snapshot × SEO | NOT RUN |
| Auth × Session × Audit | PARTIAL: Owner Audit表示、direct/reload欠陥を発見。role removal/stale cookie未実行 |
| Publish failure × rollback | NOT RUN |
| Media GC × pending revision × public release | NOT RUN |
| Signup × Legal × Email × Security | NOT RUN |
| PDF × public URL | Stage A API生成のみ。Stage B rendered PDF/QR/各format未実行 |

## Exploratory / abuse

- Rapid/double Map submit: 初回欠陥中はmutation自体が発生しなかった。
- Upload processing中にFloor追加を押すと「アップロード済みの画像を選択してください。」と明示され、処理完了後は同じ入力を安全に作成できた（PASS）。
- Mixed ja/en、zero photo、zero Category開始、required Floor/Spot name UI、reload後永続化を確認。
- long values、100 Spots、MIME mismatch、oversize、token reuse/expiry、tampered IDs、stale sessions、publish/analytics failure、rapid locale/mobile resize、long PDF legendは未実行。

## Visual QA evidence

ブラウザsession内で次をcapture/visual reviewした。

- OWNER dashboard（1280×720）: sidebar/card/公開badgeにoverflow、clip、overlapなし。
- Map creation form: focus ring、必須表示、CTA contrastを確認。
- Floor manager/upload preview: preview/processing/success/errorが識別可能。登録後listに画像、georeference、Decoration actionsが収まる。
- Category manager: icon selector、usage、translation欄を確認。
- Spot create/detail: form spacing、PIN preset/size/importance、Media pickerを確認。ただし作成後stale screenをP2として記録。
- Organization/member management: role badge、Map assignment、destructive buttons、Audit linkを確認。
- Audit Log client-render: action/time/actor/target/metadataを確認。

指定されたSpot Editor/revision/Analytics/public desktop+mobile/Bottom Sheet/English/Terms/Privacy/onboarding/PDF pagesは未capture。このためvisual QA gateは未完了。

## Accessibility

- 実操作: semantic textbox/combobox/checkbox/button/link名、Map作成CTAの可視focus、Floor upload status/alertをAccessibility treeで確認。
- Automated: Windows `tests/accessibility.test.ts` 5/5 PASS、`tests/accessibility-source.test.ts` 1/1 PASSを含む61 files / 397 tests PASS。
- 未完了: deployed public principal statesのkeyboard-only完走、Bottom Sheet focus trap/return、Spot list alternative、desktop/mobile状態のcritical/serious axe再scan。よってStage B accessibility gateは未完了。

## Security / RBAC

- OWNER sessionでTenant A Mapsとmember managementを表示。
- Tenant B fixtureとunassigned A2を作成したが、Map EDITOR/Spot Editor browser sessionによるdirect URL/API denialとstale authorizationの実測は未実行。
- Stage A negative testsはgreenだが、Stage Bのserver rejection evidence要件を満たしていない。

## Defects

| ID | Severity | Defect | Root cause | Fix commit | QA rerun |
| --- | --- | --- | --- | --- | --- |
| AIUAT-001 | P1 | OWNERの新規Map作成が無反応 | `mapCreateSchema`へ`selectedType` ref objectを渡しliteral validationが常時失敗 | `2a12003951921f3c14dd3a080394161e788d9a71` | Windows再deploy後、UI Map作成PASS |
| AIUAT-002 | P2 | Spot作成後、URLだけ詳細へ変わり作成フォームが残る | 同一管理フローのclient route切替でpage更新が安定しない | `ff4060ceec0e3c6688af3d93bf53ce40d0b0e7ce` (`external: true`) | regression tests PASS、Windows未deploy/未rerun |
| AIUAT-003 | P2 | Audit Logのdirect URL/reloadが401（組織画面からのclient navigationは成功） | setup時のraw `$fetch`がSSR request cookieをforwardしない | `2a323ce38decac226bad9a215085a5e591234453` (`useFetch`) | regression tests PASS、Windows未deploy/未rerun |

## Automated rerun evidence / test-count discrepancy

- `2a12003`: local full 61 files / 397 tests、typecheck、production build PASS。Windows full 61 / 397、typecheck、production build、Prisma validate、28/28 migration status PASS。health `/api/health` 200、ready `/api/ready` 200（DB true/mail fake）。
- `ff4060c` and `2a323ce`: local full 61 / 397 + typecheck PASS。Windows deploy/build/rerun pending。
- Stage A差異（local 397 / Windows 396）の理由: Windows full suiteは`d24b01f`時点で実行され、その後`14f292e`でSnapshot/PDF回帰テストが1件追加された。最終追加testはWindows focused 2 files / 15 testsでPASS済み。今回`2a12003`のWindows full suiteは61 / 397となり、platform skipではなく実行SHA差だったことを確定した。
- Dependency audit known issue: Prisma config経由transitive `deepmerge-ts` High。安全でないPrisma 8 RC major overrideは未適用。

## Remaining issues / Stage B verdict

Open P0/P1はない。AIUAT-002/003はcode上修正済みだがWindows QAで未検証のcore P2であり、加えてMap EDITOR、Spot Editor、public/mobile、7つのmandatory cross-feature、Phase 1全回帰、exploratory、full visual、deployed accessibility、PDF renderが未完了。

したがってWU-40をcompleteせず、mainへmergeせず、Human UATへ進めない。

**AI-UAT NOT READY**
