# Review of the first Astra pass

The earlier result improved reliability but looked like a generated report. The assumption that readable typography plus a two-column grid would provide sufficient tourism-pamphlet identity was wrong. The neutral title, brown circles, thumbnail photos and thin rules made all formats similar. Photo absence collapsed the distinction further. The 10.34 MB one-page map was a real cost.

Retain: Source isolation, LIVE/PUBLISHED refresh, stale-output rejection, physical text sizes, complete pagination, preview-first mobile. Replace only through explicit templateVersion 3: graphic hierarchy, photo composition, category language, print encoding and customer picker.

Windows QA read-only access was explicitly authorized in the continuation. Retrieved only public-purpose arimatsu-fon DTO and referenced managed assets: 20 eligible Spots, 16 with photos, 6 categories, 13 distinct assets. Current deployed Windows code is 75ebb766; the implementation/comparison base remains abddcb4. QA PINs remain existing QA coordinates, not verified geography. Amusement/alpine targets use separately labeled synthetic QA fixtures and neutral locally-created media, never production seed data.
