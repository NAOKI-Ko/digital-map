<script setup lang="ts">definePageMeta({ layout: 'admin', middleware: 'auth' }); const { data } = await useFetch('/api/spot-editor/spots')</script>
<template><section><h1 class="text-2xl font-bold">担当Spot</h1><div class="mt-6 space-y-3"><NuxtLink v-for="spot in data?.spots ?? []" :key="spot.id" :to="`/admin/spot-editor/${spot.id}`" class="block rounded-xl bg-white p-5"><strong>{{ spot.name }}</strong><span class="ml-3 text-sm text-stone-500">{{ spot.floor.map.name }} · {{ spot.revisions.length ? '承認待ち' : '編集できます' }}</span></NuxtLink><p v-if="!(data?.spots.length)">担当中のSpotはありません。</p></div></section></template>
